import styled from 'styled-components';

export const MessageWrapper = styled.div`
  display: flex;
  flex-direction: row;
  align-items: center;
  color: var(--color-red);
  font-size: 1.4rem;
  font-style: normal;
  font-weight: 500;
  line-height: normal;
  padding: 0.4rem 0;
`;
