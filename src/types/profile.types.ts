export interface ProfileData {
  name: string;
  phoneNumber: string;
  email: string;
  birth: string;
  subject: string;
}

export interface StaffData {
  staffName: string;
  email: string;
  academyName: string;
  status: string;
}
